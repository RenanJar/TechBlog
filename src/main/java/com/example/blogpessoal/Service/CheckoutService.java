package com.example.blogpessoal.Service;

import com.example.blogpessoal.Model.PostagemModel;
import com.example.blogpessoal.Model.TemaModel;
import com.example.blogpessoal.Repository.TemaRepository;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;

public class CheckoutService {

   // public ResponseEntity ResponseEntity (PostagemModel postagem, TemaRepository tema){

        //if(postagem.getId() ==null ||
       //         postagem.getTema().getId()==null ||
       //         !tema.existsById(postagem.getTema().getId())){
       //     return ResponseEntity.badRequest().build();
     //   }
      //  return
   // }
//
}
